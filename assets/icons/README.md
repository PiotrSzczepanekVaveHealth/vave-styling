# Icon Assets

Raw SVG icon assets generated from Figma should be written here by `npm run import:icons`.

Expected layout:

```txt
assets/icons/<icon-name>/<weight>.svg
```

Each SVG is rendered by the package at one of the supported icon sizes: `16`, `24`, `32`, or `48`.
